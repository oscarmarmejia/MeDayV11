import 'package:flutter/material.dart';

class BarraArriba extends StatelessWidget {
  const BarraArriba({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

